import React from 'react';
import { Outlet } from 'react-router-dom';

type Props = {};

const InventoryPageLayout = (props: Props) => {
  return (
    <><Outlet /></>
  );
};

export default InventoryPageLayout;