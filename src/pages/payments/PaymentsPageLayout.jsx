import React from 'react';
import { Outlet } from 'react-router-dom';

type Props = {};

const PaymentsPageLayout = (props: Props) => {
  return (
    <><Outlet /></>
  );
};

export default PaymentsPageLayout;