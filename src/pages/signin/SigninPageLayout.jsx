import React from 'react';
import { Outlet } from 'react-router-dom';

type Props = {};

const SigninPageLayout = (props: Props) => {
  return (
    <><Outlet /></>
  );
};

export default SigninPageLayout;