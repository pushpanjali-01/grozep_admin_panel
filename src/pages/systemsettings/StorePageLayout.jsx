import React from 'react';
import { Outlet } from 'react-router-dom';

type Props = {};

const SystemSettingsPageLayout = (props: Props) => {
  return (
    <><Outlet /></>
  );
};

export default SystemSettingsPageLayout;